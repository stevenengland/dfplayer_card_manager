# Clean caches if commands like pytest behave strange

Example: After bigger reorganizations like renaming or file movings pytest might be irritated.

- reinstall the .venv
- remove all **pycache** dirs and \*.pyc files
